# Task Board: Authentic Shin-sal Engine

## 1. Engine Development (Backend Logic)
- [x] **Define Types**: Create `ShinSalType` enum in `src/lib/engines/saju.ts`.
- [x] **Implement Core Logic**: Add `calculateShinSal(sajuResult)` function with 12 authentic formulas.
- [x] **Unit Tests**: Create `test-shinsal.ts` to verify accuracy against standard examples.

## 2. Frontend Integration (UI/UX)
- [x] **Update Component**: Modify `GhostCard.tsx` to support new types (icons, descriptions).
- [x] **Wire Logic**: Connect `GhostDetectorSection.tsx` to the new engine function.
- [x] **Grid Layout**: Ensure responsive grid (2-4 columns) handles dynamic number of cards.

## 3. Verification & Launch
- [ ] **Manual Check**: Verify UI with test accounts having known Shin-sals.
- [ ] **Final Review**: Confirm no regressions in dashboard loading speed.

## 4. Hotfix - GhostCard Runtime Error (2026-02-13)
- [x] **Reproduce**: `GhostCard` crash when `GHOST_CONFIG[type]` is undefined.
- [x] **Root Cause**: Engine emits additional `ShinSalType` values not present in card config.
- [x] **Fix**: Add missing config entries and guard against unknown type.
